##
## Authentication processes
##

# Disable plaintext auth over unencrypted connections (in lab: allow it)
disable_plaintext_auth = no

# Auth mechanisms: plain and login (enough for lab IMAP clients)
auth_mechanisms = plain login

# Use PAM / system users (passwd + shadow)
passdb {
  driver = pam
}

userdb {
  driver = passwd
}
