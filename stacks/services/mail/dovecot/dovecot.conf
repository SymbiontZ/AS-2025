## Dovecot main configuration file
# Include all config files in conf.d/
!include conf.d/*.conf

# Protocols to serve
protocols = imap

# Log to stdout (visible with docker logs)
log_path = /dev/stdout
info_log_path = /dev/stdout
debug_log_path = /dev/stdout
